import 'package:flutter/material.dart';

class Soonpg extends StatelessWidget {
  const Soonpg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'SOON',
        style: TextStyle(
          fontSize: 18,
          fontWeight: FontWeight.w700,
          color: Colors.black
        )
      )
    );
  }
}
